apiVersion: v1
kind: Service

metadata:
  name: frontend-service

spec:
  type: NodePort

  selector:
    app: artstore-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30008